---
import Layout from '../layouts/Layout.astro';

const contacts = [
  {
    service: "Présidence",
    address: "Ouagadougou, Koulouba",
    phone: "+226 25 XX XX XX",
    email: "presidence@gov.bf"
  },
  {
    service: "Premier Ministère",
    address: "Ouagadougou, Zone du Bois",
    phone: "+226 25 XX XX XX",
    email: "pm@gov.bf"
  },
  {
    service: "Services aux citoyens",
    address: "Ouagadougou, Centre-ville",
    phone: "+226 25 XX XX XX",
    email: "services@gov.bf"
  }
];
---

<Layout title="Contact">
  <main class="container mx-auto px-4 py-8">
    <h1 class="text-4xl font-bold mb-8 text-primary">Contactez-nous</h1>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div class="bg-white p-6 rounded-lg shadow-md">
        <h2 class="text-2xl font-bold mb-6 text-accent">Formulaire de contact</h2>
        <form class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-gray-700">Nom complet</label>
            <input type="text" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm" required />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700">Email</label>
            <input type="email" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm" required />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700">Message</label>
            <textarea rows="4" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm" required></textarea>
          </div>
          <button type="submit" class="bg-accent text-white px-4 py-2 rounded-md hover:bg-opacity-90">
            Envoyer
          </button>
        </form>
      </div>

      <div class="space-y-6">
        {contacts.map(contact => (
          <div class="bg-white p-6 rounded-lg shadow-md">
            <h3 class="text-xl font-bold mb-4 text-accent">{contact.service}</h3>
            <ul class="space-y-2 text-gray-700">
              <li>📍 {contact.address}</li>
              <li>📞 {contact.phone}</li>
              <li>📧 {contact.email}</li>
            </ul>
          </div>
        ))}
      </div>
    </div>
  </main>
</Layout>